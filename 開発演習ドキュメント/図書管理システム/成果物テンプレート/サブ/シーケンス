新規会員登録
  情報を入力
  会員のサーブレットへ情報を送る（アクションを検索にする）
  サーブレットは入力値が変もしくはDAOで情報を照合してすでに存在する名前とメアドだったらエラーページへ飛ばす
  サーブレットは入力値をBeanに入れて確認画面JSPにフォワード
  確認画面で入力値を表示、戻るを押すと入力画面へ戻る
  確認画面でOKを押すと、会員のサーブレットへ入力値を送る（アクションを新規登録にする）
  サーブレットは入力値をDAOでデータベースにINSERTする（エラーが出たらエラーページへ）、登録完了ページへ飛ばす

会員検索
  会員IDかメールアドレスを選択して、検索値を入力して検索
  会員のサーブレットへ選択値と入力値を送る（アクションを検索にする）
  サーブレットはDAOで情報を照合して結果をBeanに入れて検索結果JSPへフォワード
  検索結果画面でBeanの値を表示
  会員情報変更ボタンを押すと会員情報更新画面へ遷移
    会員情報変更画面でBeanの値を表示、更新したい項目の要素を記入、確認画面へボタンで遷移
    確認画面で入力値を表示、戻るを押すと入力画面へ戻る
    確認画面でOKを押すと、会員のサーブレットへ入力値を送る（アクションを更新にする）
    サーブレットは入力値をDAOでデータベースにUPDATEする（エラーが出たらエラーページへ）、更新完了ページへ飛ばす
  会員退会ボタンを押すとサーブレットでアクションを貸し出し状況にしてBeanを送る
    サーブレットはBeanから会員IDに紐づいた貸出台帳の行数をカウントして、
    カウントが０ならBeanを退会JSPへフォワード
      退会画面でBeanの会員情報を表示
      退会ボタンを押うと会員のサーブレットへアクション退会を送る
      サーブレットはDAOでBeanの会員IDに紐づいた行を削除（エラーが出たらエラーページへ）、退会完了ページへ飛ばす
      退会完了ページを表示
    カウントが１以上ならBeanを退会不可JSPへフォワード
      退会不可ページを表示
